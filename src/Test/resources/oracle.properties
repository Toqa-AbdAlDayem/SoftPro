
oracle.jdbc.url=jdbc:oracle:thin:@localhost:1521:orcl

oracle.jdbc.username=car_db
oracle.jdbc.password=car12345
spring.main.allow-circular-references=true
# Enable static resource handling
spring.resources.add-mappings=true



# Set the path where static resources (like CSS files) are located
spring.mvc.static-path-pattern=/static/**
spring.resources.static-locations=classpath:/static/

# Disable caching for development (optional, but can help during development)
spring.resources.cache.period=0